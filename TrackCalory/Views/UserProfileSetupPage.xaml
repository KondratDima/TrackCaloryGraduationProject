<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="TrackCalory.Views.UserProfileSetupPage"
             Title="Налаштування профілю"
             BackgroundColor="#f0eaed"
             >
    <ScrollView>
        <StackLayout Padding="20" Spacing="20">

            <Label Text="👤 Створення профілю"
                   FontSize="28"
                   FontAttributes="Bold"
                   HorizontalOptions="Center"
                   Margin="0,20,0,10"/>

            <Label Text="Для розрахунку денної норми калорій заповніть дані:"
                   FontSize="14"
                   HorizontalOptions="Center"
                   TextColor="Gray"/>

            <!-- Стать -->
            <Frame BackgroundColor="White" HasShadow="True" CornerRadius="10" Padding="15">
                <StackLayout>
                    <Label Text="Стать" FontAttributes="Bold"/>
                    <Picker x:Name="GenderPicker" Title="Оберіть стать" SelectedItem="Чоловік">
                        <Picker.ItemsSource>
                            <x:Array Type="{x:Type x:String}">
                                <x:String>Чоловік</x:String>
                                <x:String>Жінка</x:String>
                            </x:Array>
                        </Picker.ItemsSource>
                    </Picker>
                </StackLayout>
            </Frame>

            <!-- Вага -->
            <Frame BackgroundColor="White" HasShadow="True" CornerRadius="10" Padding="15">
                <StackLayout>
                    <Label Text="Вага (кг)" FontAttributes="Bold"/>
                    <Entry x:Name="WeightEntry" 
                           Keyboard="Numeric" 
                           Placeholder="Наприклад: 70"/>
                </StackLayout>
            </Frame>

            <!-- Зріст -->
            <Frame BackgroundColor="White" HasShadow="True" CornerRadius="10" Padding="15">
                <StackLayout>
                    <Label Text="Зріст (см)" FontAttributes="Bold"/>
                    <Entry x:Name="HeightEntry" 
                           Keyboard="Numeric" 
                           Placeholder="Наприклад: 175"/>
                </StackLayout>
            </Frame>

            <!-- Вік -->
            <Frame BackgroundColor="White" HasShadow="True" CornerRadius="10" Padding="15">
                <StackLayout>
                    <Label Text="Вік (років)" FontAttributes="Bold"/>
                    <Entry x:Name="AgeEntry" 
                           Keyboard="Numeric" 
                           Placeholder="Наприклад: 25"/>
                </StackLayout>
            </Frame>

            <!-- Рівень активності -->
            <Frame BackgroundColor="White" HasShadow="True" CornerRadius="10" Padding="15">
                <StackLayout>
                    <Label Text="Рівень активності" FontAttributes="Bold"/>
                    <Picker x:Name="ActivityPicker" Title="Оберіть рівень" SelectedItem="Легка активність (1-3 дні/тиждень)">
                        <Picker.ItemsSource>
                            <x:Array Type="{x:Type x:String}">
                                <x:String>Сидячий спосіб життя</x:String>
                                <x:String>Легка активність (1-3 дні/тиждень)</x:String>
                                <x:String>Помірна активність (3-5 днів/тиждень)</x:String>
                                <x:String>Активний (6-7 днів/тиждень)</x:String>
                                <x:String>Дуже активний (фізична робота)</x:String>
                            </x:Array>
                        </Picker.ItemsSource>
                    </Picker>
                </StackLayout>
            </Frame>

            <!-- Мета -->
            <Frame BackgroundColor="White" HasShadow="True" CornerRadius="10" Padding="15">
                <StackLayout>
                    <Label Text="Ваша мета" FontAttributes="Bold"/>
                    <Picker x:Name="GoalPicker" Title="Оберіть мету" SelectedItem="Схуднення">
                        <Picker.ItemsSource >
                            <x:Array Type="{x:Type x:String}">
                                <x:String>Схуднення</x:String>
                                <x:String>Підтримка ваги</x:String>
                                <x:String>Набір м'язової маси</x:String>
                            </x:Array>
                        </Picker.ItemsSource>
                    </Picker>
                </StackLayout>
            </Frame>

            <!-- Кнопка розрахунку -->
            <Button Text="💪 Розрахувати та зберегти"
                    BackgroundColor="#5fd37c"
                    TextColor="White"
                    FontSize="18"
                    HeightRequest="60"
                    CornerRadius="30"
                    Clicked="OnCalculateClicked"
                    Margin="0,20,0,0"/>

        </StackLayout>
    </ScrollView>
</ContentPage>